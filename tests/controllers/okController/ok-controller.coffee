module.exports =

	options:

		baseRoute: '/base'

	endpoint:

		route: '/endpoint'

		method: 'post'

		middleware: [ 
			(req, res, next) ->

			(req, res, next) ->
		]

		handler: (req, res) ->
			